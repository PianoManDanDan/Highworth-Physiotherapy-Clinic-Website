import React from 'react';
import { render } from '@testing-library/react';
import {
  {{pascalCase name}},
  {{pascalCase name}}Props,
} from '../../../components/{{pascalCase name}}';

describe('{{pascalCase name}} Component snapshot test', () => {
  test('it renders the component correctly', () => {
  // Arrange
  const {{camelCase name}}Data: {{pascalCase name}}Props = {

  };

  const { asFragment } = render(
    <{{pascalCase name}} {...{{camelCase name}}Data} />
  );

  // Act
  const rendered{{pascalCase name}} = asFragment();

  // Assert
  expect(rendered{{pascalCase name}}).toMatchSnapshot();
  });
});
