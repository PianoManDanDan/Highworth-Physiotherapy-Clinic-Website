import React from 'react';
import { render } from '@testing-library/react';
import '@testing-library/jest-dom/extend-expect';
import 'jest-extended';
import { Map } from '../src';

describe.skip('Map Component', () => {
	it('renders without crashing', () => {
		
	});
});